Uses Intrervention Library

#Requires GD or imagick extension, set by Image::configure(array('driver' => 'gd'));  imagick works on localhost, DOES NOT work on ZZZ.com.ua
#Requires php fileinfo extension, in php-ini removed ";" before extension=php_fileinfo.dll